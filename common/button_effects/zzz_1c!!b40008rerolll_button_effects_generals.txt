rerolll_button_effects_generals = {
    potential = {
        has_global_flag = z_modmenu_installed
    }
    allow = {
        hidden_trigger = {
            NOT = {
                has_country_flag = z_mui_flg_fta_cd_general
            }
        }
    }
    effect = {
        hidden_effect = {
            custom_tooltip
            "fta.5.name"
            country_event = {
                id = fire_them_all.5
            }
        }
    }
}
